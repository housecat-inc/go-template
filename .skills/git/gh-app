#!/bin/bash
SOURCE="${BASH_SOURCE[0]}"
while [ -L "$SOURCE" ]; do
  SOURCE="$(readlink "$SOURCE")"
done
SCRIPT_DIR="$(cd "$(dirname "$SOURCE")" && pwd)"
TOKEN=$("$SCRIPT_DIR/gh-auth.sh")
echo "$TOKEN" | gh auth login --with-token 2>/dev/null
gh "$@"
